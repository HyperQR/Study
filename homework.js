// get the difference between number a and b and add that to the second number for return

// if number a is bigger than number b use subtraction instead

function bob(num1, num2){
    if(){
        
    }
}

bob();
